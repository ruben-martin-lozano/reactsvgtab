import React from 'react'
import PropTypes from 'prop-types'

const Tab = ({tab}) => {
  return (
    <div className='sb-Tabº'>
      <h1>Hello World!</h1>
    </div>
  )
}

Tab.propTypes = {
  tab: PropTypes.array.isRequired
}

export default Tab
